name: Build container image
on:
  pull_request:
    branches:
      - main
  schedule:
    - cron: '05 * * * *'  # Runs 5 minutes past every hour
  push:
    branches:
      - main
    paths-ignore:
      - '**/README.md'
  workflow_dispatch:

env:
  IMAGE_REGISTRY: "ghcr.io/${{ github.repository_owner }}"  # do not edit

concurrency:
  group: ${{ github.workflow }}-${{ github.ref || github.run_id }}-${{ inputs.brand_name}}-${{ inputs.stream_name }}
  cancel-in-progress: true

jobs:
  build_push:
    name: Build and push image
    runs-on: ubuntu-24.04
    defaults:
      run:
        working-directory: 'FurryOS Gaming'

    permissions:
      contents: read
      packages: write
      id-token: write

    steps:
      # These stage versions are pinned by https://github.com/renovatebot/renovate
      - name: Checkout
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7

      - name: Maximize build space
        uses: ublue-os/remove-unwanted-software@695eb75bc387dbcd9685a8e72d23439d8686cba6

      - name: Install just
        uses: extractions/setup-just@53165ef7e734c5c07cb06b3c8e7b647c5aa16db3 # v4.0.0

      - name: Check Just Syntax
        shell: bash
        run: |
          just check

      - name: Image Name
        id: image-name
        run: |
          IMAGE_NAME=$(just image_name)
          echo "IMAGE_NAME=${IMAGE_NAME}" >> $GITHUB_ENV

      - name: Default Tag
        id: gen-default-tag
        run: |
          DEFAULT_TAG=$(just generate-default-tag)
          echo "DEFAULT_TAG=${DEFAULT_TAG}" >> $GITHUB_ENV

      - name: Prepare environment
        run: |
          # Lowercase the image uri https://github.com/macbre/push-to-ghcr/issues/12
          echo "IMAGE_REGISTRY=${IMAGE_REGISTRY,,}" >> ${GITHUB_ENV}
          echo "IMAGE_NAME=${IMAGE_NAME,,}" >> ${GITHUB_ENV}

      # Check if today's image was already built and published
      - name: Check if today's build succeeded
        id: check-today
        if: github.event_name == 'schedule'
        run: |
          TODAY_TAG=$(date +%Y%m%d)
          if podman pull "${IMAGE_REGISTRY}/${IMAGE_NAME}:${TODAY_TAG}" &>/dev/null; then
            echo "Build for today (${TODAY_TAG}) already exists. Skipping hourly execution..."
            echo "skip=true" >> $GITHUB_OUTPUT
          else
            echo "No build found for today. Proceeding with build..."
            echo "skip=false" >> $GITHUB_OUTPUT
          fi

      # This actually builds the container image
      - name: Build Image
        id: build-image
        if: steps.check-today.outputs.skip != 'true'
        run: |
          sudo -E $(command -v just) build \
            ${IMAGE_NAME} \${DEFAULT_TAG}

      # This is optional, this provides smaller delta updates
      - name: Rechunk with rpm-ostree
        id: rechunk
        if: steps.check-today.outputs.skip != 'true'
        run: |
          sudo -E $(command -v just) ostree-rechunk \
            ${IMAGE_NAME} \${DEFAULT_TAG}

      - name: Generate Build Tags
        id: gen-build-tags
        if: steps.check-today.outputs.skip != 'true'
        run: |
          alias_tags="$(just generate-build-tags \
                          ${IMAGE_NAME} \${DEFAULT_TAG})"

          echo "Tags for this Action..."
          echo "${alias_tags}"
          echo "alias_tags=${alias_tags}" >> $GITHUB_OUTPUT

      - name: Tag Image
        id: tag-images
        if: steps.check-today.outputs.skip != 'true'
        env:
          ALIAS_TAGS: ${{ steps.gen-build-tags.outputs.alias_tags }}
        run: |
          sudo -E $(command -v just) tag-images \
            "${IMAGE_NAME}" \
            "${DEFAULT_TAG}" \
            "${ALIAS_TAGS}"

      - name: Login to GitHub Container Registry
        uses: docker/login-action@371161bbe7024a29a25c5e19bfcbc0804fe9ad2c # v4.5.2
        if: github.event_name != 'pull_request' && github.ref == format('refs/heads/{0}', github.event.repository.default_branch) && steps.check-today.outputs.skip != 'true'
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Push To GHCR
        if: github.event_name != 'pull_request' && github.ref == format('refs/heads/{0}', github.event.repository.default_branch) && steps.check-today.outputs.skip != 'true'
        id: push-image
        env:
          ALIAS_TAGS: ${{ steps.gen-build-tags.outputs.alias_tags }}
        run: |
          set -euox pipefail
          for tag in ${ALIAS_TAGS};
          do
            sudo -E podman push --digestfile=/tmp/digestfile ${IMAGE_NAME}:${tag}${IMAGE_REGISTRY}/${IMAGE_NAME}:${tag}
          done

          digest=$(< /tmp/digestfile)

          echo "digest=${digest}" >> $GITHUB_OUTPUT

      - name: Install Cosign
        uses: sigstore/cosign-installer@6f9f17788090df1f26f669e9d70d6ae9567deba6 # v4.1.2
        if: github.event_name != 'pull_request' && github.ref == format('refs/heads/{0}', github.event.repository.default_branch) && steps.check-today.outputs.skip != 'true'
        with:
          cosign-release: 'v2.6.3'

      - name: Sign container image
        id: sign-image
        if: github.event_name != 'pull_request' && github.ref == format('refs/heads/{0}', github.event.repository.default_branch) && steps.check-today.outputs.skip != 'true'
        env:
          COSIGN_EXPERIMENTAL: false
          COSIGN_PRIVATE_KEY: ${{ secrets.SIGNING_SECRET }}
          DIGEST: ${{ steps.push-image.outputs.digest }}
        run: |
          cosign sign -y --key env://COSIGN_PRIVATE_KEY ${IMAGE_REGISTRY}/${IMAGE_NAME}@${DIGEST}

      # -----------------------------------------------------------------
      # Formatted Messages & Summaries
      # -----------------------------------------------------------------

      - name: Success Summary
        if: success() && steps.check-today.outputs.skip != 'true'
        run: |
          cat << 'EOF' >> $GITHUB_STEP_SUMMARY
          # 🎉 Build & Push Succeeded!

          > **Repository:** `${{ github.repository }}`
          > **Branch:** `${{ github.ref_name }}`
          > **Image:** `${{ env.IMAGE_REGISTRY }}/${{ env.IMAGE_NAME }}:${{ env.DEFAULT_TAG }}`
          > **Run ID:** [${{ github.run_id }}](${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }})

          ### 📦 Tags Published
          ```text
          ${{ steps.gen-build-tags.outputs.alias_tags }}
          ```
          EOF

      - name: Failure Summary & Annotation
        if: failure()
        run: |
          cat << 'EOF' >> $GITHUB_STEP_SUMMARY
          # 🚨 Build Failed

          > **Workflow:** `${{ github.workflow }}`
          > **Repository:** `${{ github.repository }}`
          > **Branch:** `${{ github.ref_name }}`
          > **Trigger:** `${{ github.event_name }}`
          > **Run Details:** [View Failed Run](${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }})

          ---

          ### 🔍 What to do next:
          1. Review the step logs above for error details.
          2. Check `FurryOS Gaming/Justfile` recipes for syntax or build command issues.
          3. Trigger a manual run via **Actions → Run workflow** after fixing.
          EOF

          echo "::error title=Build Failed!::Workflow '${{ github.workflow }}' failed. Check the Job Summary page for details."
          
